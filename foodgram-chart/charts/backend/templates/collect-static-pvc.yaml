apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc.collect-static.name }}
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - {{ .Values.pvc.collect-static.accessModes }}
  resources:
    requests:
      storage: {{ .Values.pvc.collect-static.storage }}